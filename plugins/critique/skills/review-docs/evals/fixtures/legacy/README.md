# recon

Reconciles daily payment batches against the ledger.

## Quick start

```bash
npm install
cp .env.example .env
npm run dev
```

You should see `recon listening on :8080`.

## Requirements

- Node 20+
- Postgres 15+
